package logger

import (
	"testing"
)

func TestLogInit(t *testing.T) {
	//logger := zaptest.NewLogger(t)
	//zap.ReplaceGlobals(logger)
	//
	//LogInit()
	//
	//assert.Equal(t, zapcore.InfoLevel, logger.Core().Enabled(zap.InfoLevel))
	//assert.NotNil(t, Log)
	//
	//defer zap.ReplaceGlobals(zap.NewNop())
}
